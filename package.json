{
  "name": "creator-roster",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve public -p 3000",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
